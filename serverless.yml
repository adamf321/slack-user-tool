service: aws-node-project

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs14.x

functions:
  webhook:
    handler: src/handlers/webhook.handler
    events:
      - http:
          path: webhook
          method: post
          cors: true

# constructs:
#     user-event-queue:
#         type: queue
#         worker:
#             handler: src/user-event.handler

package:
  patterns:
    - "!tests/**"

plugins:
  - serverless-plugin-typescript
  # - serverless-lift
  - serverless-offline
